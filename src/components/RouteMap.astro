---
import { MIN_WIDTH, MAX_WIDTH, hue, extend } from '../loaders/gpx-utils'

interface Segment {
  x1: number
  y1: number
  x2: number
  y2: number
  speed: number
  ele: number
  hr: number | null
}
interface Props {
  svgSegments: Segment[]
  viewBox: string
}
const { svgSegments, viewBox } = Astro.props

const hrs = svgSegments.map((s) => s.hr).filter((h): h is number => h !== null)
const minHr = Math.min(...hrs)
const maxHr = Math.max(...hrs)
const hrRange = maxHr - minHr || 1

const speeds = svgSegments.map((s) => s.speed)
const minSpeed = Math.min(...speeds)
const maxSpeed = Math.max(...speeds)
const speedRange = maxSpeed - minSpeed || 1

const hasHr = hrs.length > 0

const OPACITY = 0.25

// higher HR = thicker
const lineWidth = (hr: number | null) => {
  if (hr === null || !hasHr) return MIN_WIDTH
  return (
    (Math.round(
      (MIN_WIDTH + ((hr - minHr) / hrRange) * (MAX_WIDTH - MIN_WIDTH)) * 10
    ) /
      10) *
    Math.random() *
    2
  )
}
---

<div class="route-map">
  <svg viewBox={viewBox} xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
    {
      svgSegments.map((s) => {
        const e = extend(s)
        return (
          <line
            x1={e.x1}
            y1={e.y1}
            x2={e.x2}
            y2={e.y2}
            stroke={`hsl(${hue(s.speed, minSpeed, speedRange)},60%,52%)`}
            stroke-width={lineWidth(s.hr)}
            stroke-linecap="round"
            opacity={OPACITY}
          />
        )
      })
    }
  </svg>
</div>

<style>
  .route-map {
    background: var(--bg-color);
    overflow: hidden;
    margin: 0;
    width: 100vw;
    margin-left: calc(50% - 50vw);
  }
  svg {
    display: block;
    width: 100%;
    height: auto;
  }
</style>
